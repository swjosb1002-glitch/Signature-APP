{
  "name": "signature-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Email signature builder with live preview and export",
  "main": "server.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "serve": "node server.js",
    "dev": "PORT=3001 concurrently \"npm run serve\" \"npm start\""
  },
  "proxy": "http://localhost:3001",
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "^5.0.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
